package com.example.model.card;

import com.example.model.game.place.Place;

public class WeatherCard extends CardData {
    public WeatherCard(Place place, Ability ability, com.example.model.card.enums.CardData cardData) {
        super(place, ability, cardData);
    }
}
